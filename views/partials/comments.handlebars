{{#each this}}
<article class="card">
    <div class="card-body">
        <h6 class="card-subtitle m2 muted">Created by {{user.username}} on {{format_date created_at}}</h6>
        <p class="card-text">{{comment_text}}</p>
    </div>
</article>
{{/each}}